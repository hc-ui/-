# 40 · 对齐为什么对齐到半夜（加长）

普通短视频 / 知识口播。草稿 `/workspace/.abroll-cloud/40-long`。覆盖 31.8s 短切。

- **成片**：`成片/40-对齐为什么对齐到半夜.mp4`
- **云端 only**：不写盘符，不传 Drive，无 freeze-pad

## 口播

大家好。对齐对齐到半夜。不是人不够认真。是没有人能拍板。没有拍板，就散不了。对齐不是把人留下。对齐是把决定带走。第一步，先写现在必须对齐的那一句。别把十个问题捆一起。一次只对齐一句。写不清的议题，先别开会。第二步，每人只带一个分歧上来。分歧一多，就会变成聊天。聊天可以过夜。决定不能过夜。第三步，到点没有共识。就由负责人先定七天。先定不是永远定。七天后再改，也比散不了强。散会之前，必须带走一个决定。不定，就还会再对齐到半夜。按这三步对齐。人才回得了家。

## 规格

1080×1920，24 fps，H.264 + AAC 44100 stereo，约 49.25 秒。镜头 12 条。
