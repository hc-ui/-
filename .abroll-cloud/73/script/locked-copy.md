# 73 锁定口播（NEW · 四十到五十秒）

来源：`topics-batch4.md` #73。十八句。只打「整数转换套输入会炸，先包一层」，不讲空字典出生地，不拆函数，不跳异常整章，不跳语言。

大家好。整数转换套输入会炸，先包一层。不是菜单坏了，是生输入直接转成了整数。键盘敲一个甲，程序当场炸掉。空串和字母都会炸。只打这一个炸点，异常整章先放下。先把输入包住，再拿去当选项。包一层，就是先收字，再认数字。认不出来就重问，别让程序死掉。认出来了，再交给菜单。选项一吃到的，必须是过关后的数字。没过关的甲，不准进分支。对照只打这一点。先包一层，再当选项。写菜单之前，先问输入过没过关。一敲甲就炸，是因为没包。包过一层再进选项，菜单才能站住。没包一层的输入，请当成会炸。

## 五拍

1. **钩子**：整数转换套输入会炸，先包一层。
2. **诊断**：生输入直接转整数；敲「甲」当场炸；空串和字母也会炸。
3. **做法**：先包一层——先收字，再认数字；认不出就重问。
4. **对照**：选项一只吃过关后的数字；没过关的甲不准进分支。
5. **收束**：写菜单之前先问过没过关；没包一层的输入请当成会炸。

口播禁：空字典出生地、append、从值找键、菜单拆函数、异常整章、C++、ROS2、下一期、时长、文件名、链接、真名。
