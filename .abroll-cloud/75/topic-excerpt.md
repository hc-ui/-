# topics-batch4.md #75 → 成片 75（嵌套字典，首拍）

来源：`.abroll-cloud/topics-batch4.md` 第 75 条。`成片/75-*.mp4` 此前不存在。NEW only，不重拍。

## 75. 嵌套字典别当一层用

- **A-roll 角度**：未完成 Python。不足第 3 条：`d[name] = {'语文': x, '库存': n}` 是把名字当第一层、科目或库存当第二层，不能当成一层字典。本条只打嵌套，不讲 append（42 已拍），不讲从值找键（13 已带过）。
- **B-roll 想法**：一层抽屉硬塞两层标签对不上；对照：名字 → 科目 → 分数；错把整层当成一个值。
- **来源笔记**：Drive `学习与职业规划基线.md`（2026-09-09 不足第 3 条）
- **成片名**：`75-嵌套字典.mp4`（用户指定短名；官方建议名 `75-嵌套字典别当一层用.mp4` 不占用）

### 源笔记摘录（只读）

不足第 3 条原文：嵌套字典。`d[name] = {'单价': x, '库存': n}` 左边变量当键、右边再一本小字典，需要两层 `[]` 才取到里面的字段。
