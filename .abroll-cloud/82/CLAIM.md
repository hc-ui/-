# 82 选题认领（防撞）

- **集号**：82
- **状态**：NEW only。`成片/82-*.mp4` 此前不存在。不重做 00–81，不拷短切。
- **来源**：`topics-batch4.md` 第 82 条；Drive 只读 `学习与职业规划基线.md`（2026-09-09 不足第 6 条）
- **一句话**：空容器还没当成 `if not d` 来拦；空班级直接 `max([])` 或除以 0 会崩。先问空不空，空了就停，有人再统计。
- **成片名**：`82-if-not.mp4`（用户指定）
- **时长**：目标四十到五十秒（硬限三十到六十）。不定格、不慢放注水。文档时长用中文「秒」。
- **只打这一条**：空容器先用 `if not` 拦住。不讲容器出生地，不讲 `len` 公式课。
- **明确不用 / 与已拍错开**：空字典写在循环外面（成片 64，出生地）；列表用 append（成片 42）；字典靠名字（成片 13）；int(input) 先包一层（成片 73）；菜单拆成函数（#74）；嵌套字典（#75）；大案例先拆到能退出（#87）；成绩先存盘（#86）；异常整章、C++ / ROS2
- **不做**：仙侠/短剧/`drama-pipeline`；禁止 `C:\` `D:\` `G:\`；禁止 Drive 上传；口播不说「下一期」、不报时长/文件名
- **工程**：`/workspace/.abroll-cloud/82/`
- **类型**：普通短视频 / 知识口播（白底小灯 A-roll + 黑底对照卡 B-roll）
- **云端 only**：A-roll + B-roll，不是剧情短剧。只打「空了还硬算」这一个炸点。
